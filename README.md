1. Ejecute:
$ docker compose up --build -d


2. Inserte datos en la BD usando:
el archivo init.sql y un programa como pgAdmin4 o tablePlus

El nombre de la BD, usuario, contraseña y puerto son:
POSTGRES_DB: sw2_erp
POSTGRES_USER: admin
POSTGRES_PASSWORD: adminpass
PORTS: 5432

3. Acceda a 
127.0.0.1:3000  -> para ver el front del ERP